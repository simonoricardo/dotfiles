session: idp

root: ~/Programming/Work/idp/dev/

windows:
  - name: code
    commands:
      - nvim
  - name: front
    root: app
    commands:
      - make start
  - name: back
    root: api
    commands:
      - doppler run make start
